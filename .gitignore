# Secrets / config
.env
.env.*
config.json
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# macOS
.DS_Store
# Finder / cloud-sync duplicate artifacts — directories ("companies 2/") AND
# files ("about 2.html", "llms 2.txt", "OFFICIALS_UPDATE 2.md"). These once
# shipped into web/ and got served + indexed as stale pages; never commit
# them. The build_site.py guard also fails the build if a junk dir survives.
* [0-9]/
* copy/
* copy [0-9]/
* [0-9].*
* copy.*
* copy [0-9].*

# Streamlit
.streamlit/secrets.toml

# Cached daily Reddit snapshot (regenerated at runtime)
reddit_corpus.parquet

# Local analytics + newsletter subscribers (contains PII — never commit)
data/analytics/

# Facilities-research generated output (regenerate with src/research/dc_finder.py)
research_output/

# npm package pulled only to extract SVG state outlines into src/us_map_data.py
node_modules/
