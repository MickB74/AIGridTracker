# Local research tooling only — NOT the site build.
#
# `python3 build_site.py` and every script in scripts/ run off
# requirements-build.txt, which is what CI installs. Nothing here is needed to
# build or deploy the site.
#
# This file covers the two standalone CLIs that live outside the build path:
#   * src/research/dc_finder.py — discover a company's data-center locations
#     from SEC filings and first-party pages (writes research_output/)
#   * scripts/make_og_images.py — redraw the committed OG cards
#
# The Streamlit app was retired in August 2026; streamlit, altair, plotly,
# pydeck and pyarrow were removed with it. Do not add streamlit back — see
# CLAUDE.md.
pandas>=2.0
requests>=2.28
beautifulsoup4>=4.12   # src/research: parse SEC filing / first-party page HTML
lxml>=5.0              # src/research: pandas.read_html table extraction
Pillow>=10.0           # scripts/make_og_images.py (local-only, absent from CI)
